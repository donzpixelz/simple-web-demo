services:
  web:
    image: chipsterz/simple-web-demo:latest   # pull the image built by GitHub Actions
    container_name: simple-web-demo
    ports:
      - "8080:80"
    restart: unless-stopped
    pull_policy: always                       # always pull latest on restart
